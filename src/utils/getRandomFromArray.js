
const getRandomFromArray = (arr) => {
    return arr[Math.floor(Math.random() * arr.length)]
}

export default getRandomFromArray

// acá se usó rafce.
// rafce proporciona la estructura básica de
// un componente, usando una función flecha